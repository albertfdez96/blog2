{% extends 'base.html.twig' %}

{% block stylesheets %}

    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/home.css') }}">
{% endblock %}

{% block body %}

    <h1>Inicio</h1>

    <p>Bienvenido {% if app.user is null %}
            <br></n>Registrate o inicia sesion{% endif %}

        {% if app.user is not null %}
            {{ app.user.username }}
        {% endif %}</p>

{% endblock %}